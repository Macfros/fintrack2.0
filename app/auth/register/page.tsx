import RegisterForm from "@/app/components/auth/RegisterForm";

interface AppProps{
   
}

const Register: React.FC<AppProps> = () => {
    return (
        <RegisterForm />
    )
}

export default Register;